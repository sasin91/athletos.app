[package]
name = "pixmyday-domain"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lib]
name = "pixmyday_domain"
path = "src/lib.rs"

[dependencies]
chrono = { version = "0.4.45", default-features = false, features = ["clock", "std"] }
chrono-tz = "0.10.4"
rrule = "0.14.0"
thiserror = "2.0.19"
